server:
  port: 8081

spring:
#  cloud:
#    compatibility:
#      verifier:
#        enabled: false
  application:
    name: USER-SERVICE  # The application name is set correctly.
  config:
    import: configserver:http://localhost:8099


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true      # Ensures registration with IP instead of hostname
    instance-id: ${spring.application.name}:${server.port}   # Custom instance ID to avoid conflicts
    lease-renewal-interval-in-seconds: 30  # Improves registration stability
    lease-expiration-duration-in-seconds: 90

logging:
  level:
    org:
      springframework: DEBUG
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html  # Ensures correct Swagger UI path

